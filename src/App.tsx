// Composition root. Filled in progressively as layers are ported (P1–P5).
export default function App() {
  return (
    <div style={{ fontFamily: "'Inter',system-ui,sans-serif", padding: 24 }}>
      Rental Property · Deal Analyzer — refactor in progress.
    </div>
  );
}
